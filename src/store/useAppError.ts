export const useAppError = () => {
  //   const userError = useUserStore((state) => state.isError)
  //   const lotteryError = useLotteryStore((state) => state.isError)
  //   const shopError = useShopStore((state) => state.isError)
  //   const preventionsError = usePreventionsStore((state) => state.isError)
  //   const boardError = useBoardStore((state) => state.isError)
  //   const taskError = useTasksStore((state) => state.isError)
  //   return userError || lotteryError || shopError || preventionsError || boardError || taskError
}
